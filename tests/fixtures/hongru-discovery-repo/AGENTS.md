# Hongru Agent Routing

Read this root file first, then the nearest scoped AGENTS.md.

Reasoning Session First: reasoning session, SSE, graph, diagnosis, and final envelope tasks must inspect the contract and backend session chain before frontend symptoms.

Quality Gate Discipline: never bypass pre-commit with --no-verify. Stage hook edits, rerun, and stop after repeated failures with a failure summary.
