# v0.6 Precise Discovery Chains & Incremental Index

v0.6 turns Discovery Mode from candidate lists into evidence chains.

## Capabilities

- `trace_feature`: route/page/handler to implementation and tests.
- Import Resolution V2 with persisted `import_bindings`.
- Duplicate cluster persistence in `similarity_clusters` and `similarity_members`.
- `explain_reuse` and `duplicate_clusters`.
- Layered `impact_analysis_v2`.
- `pnav scan --incremental` and `pnav scan --full`.

The call graph remains confidence-scored. Ambiguous results stay low-confidence rather than pretending to be exact.
